# Visual companion (brainstorming) working files
.superpowers/

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.coverage
htmlcov/

# Device secrets (WiFi creds) — never commit
settings.toml

# Server secrets (GitHub PAT etc.) — never commit
.env

# OS
.DS_Store
